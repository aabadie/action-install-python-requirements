name: 'Install Python requirements'
description: 'Install Python requirements from text file.'
inputs:
  requirements:
    description: 'requirements text file'
    required: true
    default: 'requirements.txt'
  update-pip:
    description: 'Whether pip also has to be updated'
    required: true
    default: false
runs:
  using: 'node16'
  main: 'index.js'
